#includes input(), print(), len()
#includes modules, like Math, Random, and must import them first to use them
import pyperclip
import sys
from random import *

#less readable thanks to from keyword
randint(1, 10)
#sys.exit() #imports are faded without any use. Also line below doesn't execute

print(randint(1, 18))

#Third party modules with installations, using Pit program, run from the command line.


pyperclip.copy('Pyperclip is a module you can import that allows you to copy and paste to and from the clipboard on your computer. It does this through the use of two functions: copy() and paste()... go figure!')

